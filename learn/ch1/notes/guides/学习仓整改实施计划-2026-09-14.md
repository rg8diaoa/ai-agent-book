# 学习记录仓整改实施计划（learn-repo task）— 2026-09-14

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **本计划遵守 AGENTS.md §1：全程不执行 `git commit`**（writing-plans 默认的"频繁提交"被仓库规则覆盖）；所有变更留在工作区，Task 13 产出人工复核包，commit 由学习者明确下令。

**Goal:** 按已终审的 v2 适配层版方案，把学习产物迁入 `learn/` 容器、还原全部上游文件改动、落地运行适配层（contracts + run.py），并跑通验证阶梯，交付"仅需人工复核再 commit"的工作区。

**Architecture:** 主仓保真（上游文件 0 修改）+ `learn/` 容器（按章编号）+ 运行适配层（路由解析 → 上游 env 契约槽位注入 → 以实验目录为 cwd 启动上游脚本）。

**Tech Stack:** Python 3.12（`.venv\Scripts\python.exe`）、git、PowerShell 5.1、pytest（仅上游已有的）。

**Spec:** [学习记录仓整改与发布方案.md](学习记录仓整改与发布方案.md)（v2）。执行中发现的 spec 勘误见 Task 10（含：`agentbook` 实为上游包，路由层更名 `learn/router/`）。

**关键上游事实（执行者必读，全部已实测核实）：**
1. `agentbook/` 是**上游自己的包**（`origin/main:agentbook/__init__.py + agentbook/providers/{registry,resolution,models,openrouter,legacy}.py`）；我们的 `agentbook/model_config.json`、`agentbook/model_router.py` 是加进上游包的两个外来文件 → 本计划将其移至 `learn/router/`，上游 `agentbook/` 保持原样（含其 `__init__.py`，**不动**）。
2. 上游 providers 注册表原生支持 env 覆盖：`models.py:72` `os.getenv(self.base_url_var, "") or self.base_url`；dashscope 条目 `key_vars=("DASHSCOPE_API_KEY",), base_url_var="DASHSCOPE_BASE_URL"`（`registry.py:34-35`）。
3. 上游各实验配置面（契约槽位）：
   - `chapter1/image-gen-workflow/config.py`：直读 `KIMI_API_KEY/KIMI_BASE_URL/REWRITE_MODEL`、`DASHSCOPE_API_KEY/DASHSCOPE_BASE_URL/WANX_MODEL`、`OPENAI_API_KEY/OPENAI_BASE_URL/GPT_IMAGE_MODEL`、`GEMINI_API_KEY`（validate 仅查非空）；`main.py` 有 `--route {ALL_ROUTES,all}`、`--requirement`。
   - `chapter1/search-codegen/config.py` L29-37：`OPENROUTER_*`、`DASHSCOPE_API_KEY/DASHSCOPE_BASE_URL/DASHSCOPE_MODEL`（默认 qwen3.7-plus）。
   - `chapter1/context/main.py`：`--provider <SUPPORTED_PROVIDERS>` + `resolve_backend` 走注册表；`--model/--api-key` 可显式传。
   - `chapter1/learning-from-experience/llm_agent.py` L88-115：`LLM_PROVIDER=dashscope` 分支走注册表（读 `DASHSCOPE_API_KEY/DASHSCOPE_MODEL/DASHSCOPE_BASE_URL`）。
4. `model_tests/infra/acceptance_router.py` 结构：L20-21 `ROOT/OUT_DIR`；L47/L56 import main_route；L67 import 上游 main.py 的 REQUIREMENTS（不动）；L90-104 `_run_image_route` 进程内 import 上游 pipeline；L115-128 `run_llm` subprocess。

---

### Task 0: 执行前快照

**Files:** 无改动，仅记录基线。

- [ ] **Step 0.1: 工作区干净确认**

Run: `git status --short`
Expected: 空输出（无未提交变更）。

- [ ] **Step 0.2: 记录待推提交清单（复核包要用）**

Run: `git log origin/main..main --oneline`
Expected: 5 条（79f660d7 / 26c7f1ff / 684218a5 / e1930f78 / edbf7673）。

- [ ] **Step 0.3: 路由冒烟基线确认**

Run: `.venv\Scripts\python.exe model_tests\infra\t10_router_smoke.py`
Expected: 7 条路由全绿（与方案附录 A 一致）。

---

### Task 1: 迁移学习产物（git mv）

**Files:**
- Move: `agentbook/model_router.py` → `learn/router/model_router.py`
- Move: `agentbook/model_config.json` → `learn/router/model_config.json`
- Move: `model_tests/ch1/*` → `learn/ch1/probes/`
- Move: `model_tests/infra/*` → `learn/infra/`
- Move: `model_tests/evidence_variant/*` → `learn/ch1/evidence/`
- Move: `docs/cc_learn/task0/{guides,research}/` → `learn/ch1/notes/{guides,research}/`
- Move: `docs/cc_learn/CC_Task0学习心得.docx` → `learn/ch1/notes/`
- Move: `docs/cc_learn/learn-repo/guides/*` → `learn/ch1/notes/guides/`
- Move: `chapter1/web-search-agent/main_route.py` → `learn/ch1/variants/web-search-agent/main_route.py`
- Delete: `model_tests/README.md`（Task 9 并入 learn/README.md 后删除；本任务先移到 `learn/README-old-model-tests.md` 暂存，Task 9 删除）

- [ ] **Step 1.1: 建容器目录**

```powershell
New-Item -ItemType Directory -Force -Path learn\router, learn\ch1\probes, learn\ch1\notes\guides, learn\ch1\notes\research, learn\ch1\evidence, learn\ch1\variants\web-search-agent, learn\infra | Out-Null
```

- [ ] **Step 1.2: 路由层两个文件移出上游包**

```powershell
git mv agentbook/model_router.py learn/router/model_router.py
git mv agentbook/model_config.json learn/router/model_config.json
```

Expected: 无报错；`git status --short` 显示两条 R（rename）。

- [ ] **Step 1.3: 探测/设施/证据/文档迁移**

```powershell
git mv model_tests/ch1/t1_mimo_search.py learn/ch1/probes/
git mv model_tests/ch1/t2_zhipu_search.py learn/ch1/probes/
git mv model_tests/ch1/t3_agnes_site.py learn/ch1/probes/
git mv model_tests/ch1/t4_agnes_image.py learn/ch1/probes/
git mv model_tests/ch1/t5_zhipu_search_api.py learn/ch1/probes/
git mv model_tests/ch1/t6_zhipu_responses.py learn/ch1/probes/
git mv model_tests/ch1/t6b_zhipu_url_probes.py learn/ch1/probes/
git mv model_tests/ch1/t7_agnes_responses.py learn/ch1/probes/
git mv model_tests/ch1/t7b_agnes_responses_forced.py learn/ch1/probes/
git mv model_tests/ch1/t8_mimo_responses.py learn/ch1/probes/
git mv model_tests/ch1/t9_chat_inline_probe.py learn/ch1/probes/
git mv model_tests/ch1/t9b_zhipu_citation_locate.py learn/ch1/probes/
git mv model_tests/ch1/t11_dashscope_probe.py learn/ch1/probes/
git mv model_tests/ch1/t12_wanx_probe.py learn/ch1/probes/
git mv model_tests/infra/acceptance_router.py learn/infra/
git mv model_tests/infra/probe_utils.py learn/infra/
git mv model_tests/infra/t10_router_smoke.py learn/infra/
git mv model_tests/evidence_variant/. learn/ch1/evidence/
git mv docs/cc_learn/task0/guides/模型路由与实验替换指南.md learn/ch1/notes/guides/
git mv docs/cc_learn/task0/research/模型能力核对报告.md learn/ch1/notes/research/
git mv docs/cc_learn/CC_Task0学习心得.docx learn/ch1/notes/
git mv docs/cc_learn/learn-repo/guides/学习记录仓整改与发布方案.md learn/ch1/notes/guides/
git mv chapter1/web-search-agent/main_route.py learn/ch1/variants/web-search-agent/main_route.py
git mv model_tests/README.md learn/README-old-model-tests.md
```

Expected: 全部成功；`model_tests/`、`docs/cc_learn/` 目录消失。

- [ ] **Step 1.4: 迁移完整性核对**

Run: `git status --short | Select-String '^R' | Measure-Object | Select-Object -ExpandProperty Count`
Expected: `25`（24 个 mv + README 暂存；若 evidence_variant 内有 json 文件为 R 状态则计数相应增加——以 `git status --short` 人工核对无 D/A 意外为准）。

---

### Task 2: learn 骨架（包化 + 忽略规则）

**Files:**
- Create: `learn/__init__.py`
- Create: `learn/router/__init__.py`
- Create: `learn/infra/__init__.py`
- Create: `learn/ch1/__init__.py`
- Create: `learn/.gitignore`

- [ ] **Step 2.1: 包文件（完整内容）**

`learn/__init__.py`:
```python
"""个人学习记录容器：notes / probes / evidence / variants / contracts / router / infra。"""
```

`learn/router/__init__.py`:
```python
"""任务模型路由（原 agentbook 外挂层，现为 learn 子包）。"""
from .model_router import resolve, RouterError

__all__ = ["resolve", "RouterError"]
```

`learn/infra/__init__.py`:
```python
"""探测与第二轨验收公共设施。"""
```

`learn/ch1/__init__.py`:
```python
"""第一章学习产物（contracts.py 需要本目录为可导入包）。"""
```

- [ ] **Step 2.2: `learn/.gitignore`（完整内容）**

```gitignore
# 第二轨路由变体证据：json 入库，生成的图片不入库（可由脚本重生成）
ch1/evidence/*.png
```

- [ ] **Step 2.3: 验证上游 .gitignore 已含全局 Python 忽略（不新增）**

Run: `Select-String -Path .gitignore -Pattern '__pycache__|\.venv|\.env' | Select-Object -First 5`
Expected: 有命中（上游已全局忽略；若无 `__pycache__/` 条目则在 `learn/.gitignore` 追加一行 `__pycache__/`）。

- [ ] **Step 2.4: import 冒烟（此时 model_router.py 尚未改任何内容，位置无关）**

Run: `.venv\Scripts\python.exe -c "import sys; sys.path.insert(0,'.'); from learn.router import resolve; print(resolve('text_only')['model'])"`
Expected: `glm-5.3-flash`

---

### Task 3: main_route.py 自举改造

**Files:**
- Modify: `learn/ch1/variants/web-search-agent/main_route.py:1-17`

- [ ] **Step 3.1: 替换文件头部 import（原第 1-17 行区域）**

原：
```python
from agentbook.model_router import resolve

load_dotenv()
```
改为（保留原 docstring，其余 import 不动）：
```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[4]))   # …/learn/ch1/variants/web-search-agent → ROOT

from learn.router import resolve

load_dotenv()
```

- [ ] **Step 3.2: 语法自检**

Run: `.venv\Scripts\python.exe -m py_compile learn\ch1\variants\web-search-agent\main_route.py`
Expected: 退出码 0，无输出。

---

### Task 4: 探测脚本样板头更新（14 个文件）

**Files:**
- Modify: `learn/ch1/probes/t*.py`（t1,t2,t3,t4,t5,t6,t6b,t7,t7b,t8,t9,t9b,t11,t12）

- [ ] **Step 4.1: 先核实现状模式一致性**

Run: `Select-String -Path learn\ch1\probes\t*.py -Pattern 'model_tests' | ForEach-Object { "$($_.Filename):$($_.LineNumber): $($_.Line.Trim())" }`
Expected: 列出各文件旧引导行（`parents[2] / "model_tests" / "infra"`）与 `from probe_utils import`。逐一记录与标准模式的差异（若有）。

- [ ] **Step 4.2: 批量替换（PowerShell）**

```powershell
Get-ChildItem learn\ch1\probes\t*.py | ForEach-Object {
  $c = Get-Content $_.FullName -Raw -Encoding UTF8
  $c = $c -replace 'sys\.path\.insert\(0, str\(Path\(__file__\)\.resolve\(\)\.parents\[2\] / "model_tests" / "infra"\)\)', 'sys.path.insert(0, str(Path(__file__).resolve().parents[3]))'
  $c = $c -replace 'from probe_utils import', 'from learn.infra.probe_utils import'
  Set-Content $_.FullName $c -Encoding UTF8 -NoNewline
}
```

Step 4.1 中发现的不符合标准模式的文件：人工按同一语义修改（旧意图 = 把 infra 目录加进 sys.path；新语义 = 把 ROOT 加进 sys.path 且 learn/infra 为包）。

- [ ] **Step 4.3: 残留验证**

Run: `Select-String -Path learn\ch1\probes\t*.py -Pattern 'model_tests|^from probe_utils'`
Expected: 空输出。

- [ ] **Step 4.4: 全部编译自检**

Run: `Get-ChildItem learn\ch1\probes\t*.py | ForEach-Object { .venv\Scripts\python.exe -m py_compile $_.FullName; if ($LASTEXITCODE -ne 0) { "FAIL: $_" } }; "compile check done"`
Expected: 仅输出 `compile check done`（无 FAIL）。

---

### Task 5: acceptance_router.py 与 probe_utils.py 更新

**Files:**
- Modify: `learn/infra/acceptance_router.py`（L1-9 docstring、L20-21、L47、L56、L90-104、L120）
- Modify: `learn/infra/probe_utils.py`（L1-10 docstring；L15 逻辑不变）

- [ ] **Step 5.1: acceptance_router.py docstring（L1-9）替换为**

```python
"""第二轨验收（路由变体）：用 learn/router 路由配置跑各实验核心流程，产出 evidence_*。

与作者基线分轨记录、永不混同：
- 第一轨：chapter1/**/validation/（作者 kimi/moonshot/qwen 观测，锚定 runner 产出）
- 第二轨：learn/ch1/evidence/（路由 provider/model + 耗时 + 产物摘要；经 learn/ch1/contracts.py 契约注入）
用法：
  .venv\\Scripts\\python.exe learn\\infra\\acceptance_router.py builtin|react|workflow|native|llm|all
  .venv\\Scripts\\python.exe learn\\infra\\acceptance_router.py workflow --requirement "一段自定义需求文本"
"""
```

- [ ] **Step 5.2: L20-21 的 OUT_DIR 替换**

原：
```python
ROOT = Path(__file__).resolve().parents[2]
OUT_DIR = ROOT / "model_tests" / "evidence_variant"
```
改为（ROOT 行不变）：
```python
ROOT = Path(__file__).resolve().parents[2]
OUT_DIR = ROOT / "learn" / "ch1" / "evidence"
```

- [ ] **Step 5.3: L47 与 L56 的 main_route 引导路径替换（两处相同）**

原：
```python
    sys.path.insert(0, str(ROOT / "chapter1" / "web-search-agent"))
```
改为：
```python
    sys.path.insert(0, str(ROOT / "learn" / "ch1" / "variants" / "web-search-agent"))
```

- [ ] **Step 5.4: `_run_image_route`（L90-95 区域）在 import 上游模块前注入契约**

原：
```python
def _run_image_route(route: str, requirement: str) -> dict:
    sys.path.insert(0, str(ROOT / "chapter1" / "image-gen-workflow"))
    from dotenv import load_dotenv

    load_dotenv(ROOT / ".env")
    from pipeline import ROUTE_RUNNERS
```
改为：
```python
def _run_image_route(route: str, requirement: str) -> dict:
    sys.path.insert(0, str(ROOT))
    from learn.ch1 import contracts

    contracts.apply_env("image-gen-workflow")
    sys.path.insert(0, str(ROOT / "chapter1" / "image-gen-workflow"))
    from dotenv import load_dotenv

    load_dotenv(ROOT / ".env")
    from pipeline import ROUTE_RUNNERS
```
（L67 的 `sys.path.insert(0, str(ROOT / "chapter1" / "image-gen-workflow"))` 与 `from main import REQUIREMENTS` **保持不变**——上游文件未动。）

- [ ] **Step 5.5: `run_llm`（L115-122 区域）env 注入**

原：
```python
        env={**os.environ, "LLM_PROVIDER": "router"},
```
改为：
```python
        env={**os.environ, **_contracts_env("learning-from-experience")},
```
并在文件顶部 import 区之后（`OUT_DIR = ...` 行后）加：
```python
def _contracts_env(exp: str) -> dict:
    sys.path.insert(0, str(ROOT))
    from learn.ch1 import contracts

    return contracts.apply_env(exp)
```
（`apply_env` 见 Task 7；它把契约 env 写入 `os.environ` 并返回该 dict，进程内与 subprocess 两种用法统一。）

- [ ] **Step 5.6: probe_utils.py docstring（L1-10）替换为**

```python
"""probe_utils — 探测脚本公共设施（learn/infra）。

learn/chN/probes/ 下新探测的样板：
    import sys
    from pathlib import Path
    sys.path.insert(0, str(Path(__file__).resolve().parents[3]))   # probes→chN→learn→ROOT
    from learn.infra.probe_utils import require_key, post_json, responses_receipt_types, head

职责：密钥装载与断言 / SDK 响应 json 化 / 截断打印 / HTTP POST 探针 / 回执类型提取。
"""
```
（L15 `ROOT = Path(__file__).resolve().parents[2]` 与其余逻辑**不变**——learn/infra 与原 model_tests/infra 深度一致。）

- [ ] **Step 5.7: 编译自检**

Run: `.venv\Scripts\python.exe -m py_compile learn\infra\acceptance_router.py learn\infra\probe_utils.py`
Expected: 退出码 0。

---

### Task 6: 还原上游文件（主仓保真的落地）

**Files:**
- Restore: `chapter1/context/main.py`、`chapter1/image-gen-workflow/config.py`、`chapter1/image-gen-workflow/pipeline.py`、`chapter1/learning-from-experience/llm_agent.py`、`chapter1/search-codegen/config.py`、`.gitignore`

- [ ] **Step 6.1: 还原命令**

```powershell
git restore --source=origin/main -- chapter1/context/main.py chapter1/image-gen-workflow/config.py chapter1/image-gen-workflow/pipeline.py chapter1/learning-from-experience/llm_agent.py chapter1/search-codegen/config.py .gitignore
```

- [ ] **Step 6.2: 保真验证（第一道）**

Run: `git diff origin/main..main --name-status | Select-String -NotMatch '^A'`
Expected: 空输出（对上游已有文件零 M/D；origin/main..main 的 R 状态来源于 Task 1 的 mv——若出现 R 行则核对是否均为我们自己新增文件的移动）。
说明：mv 在 diff 中可能显示为 A+D 组合（对象为我们的新增文件），同样不违规；判据 = 无任何**上游原有路径**出现 M/D。

---

### Task 7: contracts.py（契约表，完整代码）

**Files:**
- Create: `learn/ch1/contracts.py`

- [ ] **Step 7.1: 写入完整内容**

```python
"""上游实验契约表（ch1）：路由解析结果 → 上游实验读取的环境变量槽位。

要点：
- 变量名是上游代码定义的"契约槽位"，与实际厂商无关（如 KIMI_API_KEY 语义 =
  上游"改写节点"的 key 槽位）；真实 provider/model 由证据 json 记录。
- 上游共享注册表 agentbook/providers/registry.py 的 key_vars / base_url_var
  均支持 env 覆盖（models.py:72）。
- apply_env(exp) 把契约写入 os.environ 并返回该 dict（进程内 import 与
  subprocess 两种用法统一）。
"""
import os
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))   # ROOT

from learn.router import resolve


def env_image_gen_workflow() -> dict:
    """1-4 image-gen-workflow：上游 config.py 直读 KIMI_*/DASHSCOPE_*/OPENAI_*。"""
    t = resolve("text_only")
    w = resolve("image_workflow")
    n = resolve("image_native")
    return {
        # 改写节点槽位（上游读 KIMI_*）
        "KIMI_API_KEY": t["api_key"],
        "KIMI_BASE_URL": t["base_url"],
        "REWRITE_MODEL": t["model"],
        # 工作流生图槽位（异步任务式，base_url 由路由覆盖）
        "DASHSCOPE_API_KEY": w["api_key"],
        "DASHSCOPE_BASE_URL": w["base_url"],
        "WANX_MODEL": w["model"],
        # 原生生图槽位
        "OPENAI_API_KEY": n["api_key"],
        "OPENAI_BASE_URL": n["base_url"],
        "GPT_IMAGE_MODEL": n["model"],
        # 上游 validate() 仅查非空；原生路线 C 不在变体轨道
        "GEMINI_API_KEY": "placeholder-not-used",
    }


def env_search_codegen() -> dict:
    """1-3 search-codegen：上游 config.py dashscope 分支读 DASHSCOPE_*。"""
    cfg = resolve("deep_research")
    return {
        "DASHSCOPE_API_KEY": cfg["api_key"],
        "DASHSCOPE_BASE_URL": cfg["base_url"],
        "DASHSCOPE_MODEL": cfg["model"],
    }


def env_learning_from_experience() -> dict:
    """7-2 learning-from-experience：上游 llm_agent.py 的 LLM_PROVIDER=dashscope
    分支走 agentbook.providers 注册表（key_vars=DASHSCOPE_API_KEY、
    base_url_var=DASHSCOPE_BASE_URL）。"""
    cfg = resolve("text_only")
    env = {
        "LLM_PROVIDER": "dashscope",
        "DASHSCOPE_API_KEY": cfg["api_key"],
        "DASHSCOPE_MODEL": cfg["model"],
    }
    if cfg["base_url"]:
        env["DASHSCOPE_BASE_URL"] = cfg["base_url"]
    return env


def contract_context() -> dict:
    """1-1 context：上游 main.py --provider 走注册表 resolve_backend。
    返回 {"env": …, "args": …}；args 为透传给上游 main.py 的默认参数。"""
    cfg = resolve("text_only")
    provider = cfg["provider"]          # 期望 "zhipu"；若上游 SUPPORTED_PROVIDERS 不含，
    return {                            # 回退 openrouter 通用槽（见 README 运行约定）。
        "env": {
            "ZHIPU_API_KEY": cfg["api_key"],
            "ZHIPU_BASE_URL": cfg["base_url"],
            "MODEL_NAME": cfg["model"],
        },
        "args": ["--provider", provider, "--model", cfg["model"]],
    }


_CONTRACTS = {
    "image-gen-workflow": {"env": env_image_gen_workflow},
    "search-codegen": {"env": env_search_codegen},
    "learning-from-experience": {"env": env_learning_from_experience},
    "context": {"env": lambda: contract_context()["env"],
                "args": lambda: contract_context()["args"]},
}


def apply_env(exp: str) -> dict:
    """把实验 exp 的契约 env 写入当前进程 os.environ，返回该 env dict。"""
    env = _CONTRACTS[exp]["env"]()
    os.environ.update(env)
    return env


def default_args(exp: str) -> list:
    """实验 exp 的默认透传参数（无则空表）。"""
    fn = _CONTRACTS[exp].get("args")
    return fn() if fn else []


def experiments() -> list:
    return sorted(_CONTRACTS)
```

- [ ] **Step 7.2: 编译 + --list 式自检（零 API 成本）**

Run: `.venv\Scripts\python.exe -c "import sys; sys.path.insert(0,'.'); from learn.ch1 import contracts; print(contracts.experiments()); env=contracts.apply_env('search-codegen'); print(sorted(env)); print('DASHSCOPE_MODEL=', env['DASHSCOPE_MODEL'])"`
Expected: `['context', 'image-gen-workflow', 'learning-from-experience', 'search-codegen']`；env 含三个 DASHSCOPE 键；`DASHSCOPE_MODEL= qwen3.7-plus`（与附录 A 基线 deep_research 一致）。

---

### Task 8: run.py（运行适配器，完整代码）

**Files:**
- Create: `learn/ch1/run.py`

- [ ] **Step 8.1: 写入完整内容**

```python
"""运行适配器：按 learn/ch1/contracts.py 契约注入环境变量，原样运行上游实验脚本。

用法：
  .venv\\Scripts\\python.exe learn\\ch1\\run.py --list
  .venv\\Scripts\\python.exe learn\\ch1\\run.py --exp image-gen-workflow -- --route workflow --requirement agi-programmer
  .venv\\Scripts\\python.exe learn\\ch1\\run.py --exp search-codegen --evidence
`--` 之后的参数原样透传给上游脚本（追加在契约默认参数之后，可覆盖同名 flag）。
"""
import argparse
import json
import os
import subprocess
import sys
from datetime import datetime, timezone
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))   # ROOT

from learn.ch1 import contracts

ROOT = Path(__file__).resolve().parents[2]
EVIDENCE_DIR = ROOT / "learn" / "ch1" / "evidence"


def main() -> int:
    parser = argparse.ArgumentParser(description="ch1 实验运行适配器（上游脚本零改动）")
    parser.add_argument("--exp", choices=contracts.experiments(),
                        help="实验名（--list 查看）")
    parser.add_argument("--list", action="store_true", help="列出实验与其路由摘要（零 API 调用）")
    parser.add_argument("--evidence", action="store_true",
                        help="收集 stdout/stderr 与路由摘要，写 learn/ch1/evidence/")
    parser.add_argument("passthrough", nargs="*",
                        help="透传给上游脚本的参数（写在 -- 之后）")
    args = parser.parse_args()

    if args.list:
        for exp in contracts.experiments():
            print(f"{exp}")
        return 0
    if not args.exp:
        parser.error("--exp 或 --list 必选其一")

    contract = contracts._CONTRACTS[args.exp]
    env = contracts.apply_env(args.exp)
    passthrough = contracts.default_args(args.exp) + args.passthrough

    entry = {
        "image-gen-workflow": ROOT / "chapter1" / "image-gen-workflow" / "main.py",
        "search-codegen": ROOT / "chapter1" / "search-codegen" / "main.py",
        "learning-from-experience": ROOT / "chapter1" / "learning-from-experience" / "experiment.py",
        "context": ROOT / "chapter1" / "context" / "main.py",
    }[args.exp]
    cwd = entry.parent
    cmd = [sys.executable, str(entry)] + passthrough

    print(f"[adapter] exp={args.exp} cwd={cwd}")
    print(f"[adapter] routes 注入完成（真实 provider/model 见 evidence json）")
    if args.evidence:
        proc = subprocess.run(cmd, cwd=str(cwd), env={**os.environ, **env},
                              capture_output=True, text=True, timeout=1800)
        EVIDENCE_DIR.mkdir(parents=True, exist_ok=True)
        ts = datetime.now(timezone.utc).strftime("%Y%m%dT%H%M%S")
        out = EVIDENCE_DIR / f"{ts}_run_{args.exp}.json"
        out.write_text(json.dumps({
            "kind": "variant", "exp": args.exp, "cmd": cmd,
            "routes": {k: v for k, v in env.items() if k.endswith(("_MODEL", "_PROVIDER")) or k == "LLM_PROVIDER"},
            "exit_code": proc.returncode,
            "stdout_tail": proc.stdout[-2000:], "stderr_tail": proc.stderr[-1000:],
            "generated_utc": datetime.now(timezone.utc).isoformat(),
        }, ensure_ascii=False, indent=2, default=str), encoding="utf-8")
        print(f"🧾 证据已写 {out.relative_to(ROOT)}")
        print(proc.stdout[-3000:])
        if proc.returncode != 0:
            print(proc.stderr[-1500:], file=sys.stderr)
        return proc.returncode

    proc = subprocess.run(cmd, cwd=str(cwd), env={**os.environ, **env})
    return proc.returncode


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 8.2: --list 自检**

Run: `.venv\Scripts\python.exe learn\ch1\run.py --list`
Expected: 四行：`context` / `image-gen-workflow` / `learning-from-experience` / `search-codegen`。

- [ ] **Step 8.3: 编译自检**

Run: `.venv\Scripts\python.exe -m py_compile learn\ch1\run.py learn\ch1\contracts.py`
Expected: 退出码 0。

---

### Task 9: learn/README.md（入口文档，完整内容）

**Files:**
- Create: `learn/README.md`
- Delete: `learn/README-old-model-tests.md`

- [ ] **Step 9.1: 写入完整内容**

````markdown
# learn/ — 个人学习记录容器

> 本目录是**个人学习记录**（非书官方内容）：按章编号组织的学习笔记、能力探测、
> 第二轨验收证据与**运行适配层**。上游文件（书稿、实验代码、配置）保持原样零修改——
> 对上游实验的一切适配经 `learn/ch1/contracts.py` 契约注入完成（主仓保真、适配外置）。

## 授权与声明

- 原书《深入理解 AI Agent：设计原理与工程实践》版权归 Bojie Li 所有，协议 Apache-2.0（见根 [LICENSE](../LICENSE)）。
- 上游文件保持上游原样；本 fork 对上游已有文件**零修改**（`git diff origin/main --name-status` 无 M/D）。
- `learn/**` 为本人新增内容 © 本人，随本仓库以 Apache-2.0 提供。
- 密钥只存根 `.env`（不入库）；任何脚本仅从环境变量读 key。

## 目录导航

| 位置 | 内容 |
|---|---|
| `chN/notes/{guides,research}/` | 学习文档（操作手册轨 / 证据核对轨） |
| `chN/probes/` | 能力探测脚本（每章编号从 t1 重新起） |
| `chN/evidence/` | 第二轨证据（acceptance_router / run.py 写入；json 入库、png 忽略） |
| `chN/variants/` | 纯新增变体入口（如 web-search-agent/main_route.py） |
| `chN/contracts.py`、`chN/run.py` | 运行适配层（上游契约槽位注入 + 统一启动器） |
| `router/` | 任务模型路由（`model_config.json` + `model_router.py`） |
| `infra/` | 探测公共设施（probe_utils）+ 第二轨验收（acceptance_router）+ 路由冒烟（t10） |

## task ↔ ch 映射表

| task | 覆盖章节/实验 | 产物位置 |
|---|---|---|
| task0 模型路由准备 | ch1 全部实验（1-1 context / 1-2 web-search-agent / 1-3 search-codegen / 1-4 image-gen-workflow / 7-1&7-2 learning-from-experience）+ 探测 t1–t12 | `ch1/notes/`、`ch1/probes/`、`router/`、`infra/` |
| learn-repo 建仓与整改 | 仓库级（learn 容器结构、适配层、保真机制） | 本 README + `ch1/notes/guides/学习记录仓整改与发布方案.md` |

## 运行适配层

```powershell
# 列出实验（零 API）
.venv\Scripts\python.exe learn\ch1\run.py --list
# 跑某实验（上游脚本原样运行；契约注入 env，cwd=实验目录）
.venv\Scripts\python.exe learn\ch1\run.py --exp image-gen-workflow -- --route workflow --requirement agi-programmer
# 带证据落盘
.venv\Scripts\python.exe learn\ch1\run.py --exp search-codegen --evidence
```

- 契约表：`ch1/contracts.py`（每个实验的"上游槽位 ↔ 路由任务"映射，变量名 = 上游代码要读的槽位，与实际厂商无关；真实 provider/model 记录在证据 json）。
- 上游共享注册表 `agentbook/providers/registry.py` 的 `key_vars`/`base_url_var` 均支持 env 覆盖。
- context 实验若上游 `SUPPORTED_PROVIDERS` 不含路由 provider 名，回退 openrouter 通用槽：
  `--provider openrouter --model openai/<model>` + `OPENROUTER_API_KEY`。

## 常跑命令（探测与验收）

```powershell
.venv\Scripts\python.exe learn\infra\t10_router_smoke.py       # 路由冒烟（改 model_config.json 后必跑）
.venv\Scripts\python.exe learn\ch1\probes\t2_zhipu_search.py   # 探针抽检
.venv\Scripts\python.exe learn\infra\acceptance_router.py all  # 第二轨验收
```

## 新增探测的约定

1. **归属**：属于哪章放 `learn/chN/probes/`；编号从 t1 重新起，文档引用写 `chN/tX`。
2. **样板**：复用 `learn/infra/probe_utils.py`；引导行统一 `sys.path.insert(0, str(Path(__file__).resolve().parents[3]))` + `from learn.infra.probe_utils import …`。
3. **密钥**：只从根 `.env` 读（`probe_utils.require_key`）；输出不含 key 明文。
4. **路径**：取仓库根一律 `ROOT = Path(__file__).resolve().parents[N]`（N = 文件到根的层级）。
````

- [ ] **Step 9.2: 删除暂存文件**

Run: `git rm learn/README-old-model-tests.md`
Expected: 成功；原 model_tests/README.md 的有效信息（目录表、约定、常跑命令）已全部并入上文。

---

### Task 10: AGENTS.md 与方案文档同步

**Files:**
- Modify: `AGENTS.md`（§2.4/§2.5/§4/§6 与 §1 表格行）
- Modify: `learn/ch1/notes/guides/学习记录仓整改与发布方案.md`（勘误）

- [ ] **Step 10.1: AGENTS.md 逐处更新（SearchReplace，保持行文风格）**

1. §1 表格行"能力探测与测试（`model_tests/**`）"→"能力探测与测试（`learn/chN/probes/**`、`learn/infra/**`）"。
2. §2.4 "测试脚本（`model_tests/**`）只从环境变量读 key"→"测试脚本（`learn/**`）只从环境变量读 key"。
3. §2.5 条目 5 改为：**主仓保真 + 适配外置 + 证据分轨**：上游文件零修改（`git diff origin/main --name-status` 对上游已有路径无 M/D）；对上游实验的适配一律经 `learn/chN/contracts.py` 契约注入（`learn/chN/run.py` 启动）；变体入口（纯新增文件）放 `learn/chN/variants/`；验收证据分两轨——作者基线（`chapter1/**/validation/` + 锚定 runner）与路由变体（`learn/infra/acceptance_router.py` 产出的 `learn/chN/evidence/`）永不混同、变体不冒充基线。
4. §4 规则指针：路由模块改为 `from learn.router import resolve`（路由层位于 `learn/router/`，勿与上游 `agentbook/` 包混淆——上游 `agentbook/providers/` 是书的共享注册表）；"测试脚本：`model_tests/`" 一段改为 `learn/`（`ch1/probes/` = t 系列探测；`infra/` = acceptance_router 第二轨验收 + probe_utils + t10 冒烟；`ch1/evidence/` = 第二轨证据）。
5. §6 "学习/研究类 md 产物一律写入 `docs/cc_learn/<task-id>/{guides,research}/`"→"一律写入 `learn/chN/notes/{guides,research}/`（仓库级元文档放 `learn/chN/notes/` 并在 `learn/README.md` 映射表登记）"。

- [ ] **Step 10.2: `.trae/rules/project_rules.md` 检查**

Run: `Get-Content .trae\rules\project_rules.md`
Expected: 仅薄指针引用 AGENTS.md 与相对链接，无需改动（若含 model_tests 字样则同步为 learn/ 路径）。

- [ ] **Step 10.3: 方案文档勘误（SearchReplace，执行期实测推翻的三处）**

1. E4 表述修正：`agentbook` editable 安装是**上游机制**（`pyproject.toml` 即为此而设；上游 `agentbook/` 含 `providers/` 注册表），我们的外挂文件已迁至 `learn/router/`；editable finder 无需任何处理。
2. 结构树与 6.1/6.3/6.5：`learn/agentbook/` → `learn/router/`；`from learn.agentbook.model_router import resolve` → `from learn.router import resolve`；新增 `learn/ch1/__init__.py`（contracts.py 需要可导入）。
3. "12 个探测文件" → "14 个探测文件"。
4. §6.5 acceptance_router 增补：`_run_image_route` import 前注入契约；`run_llm` 的 `LLM_PROVIDER=router` → 契约注入（`learning-from-experience`）。

---

### Task 11: 历史文档路径与命令替换（notes 内两 md）

**Files:**
- Modify: `learn/ch1/notes/guides/模型路由与实验替换指南.md`
- Modify: `learn/ch1/notes/research/模型能力核对报告.md`

- [ ] **Step 11.1: 定位全部旧引用**

Run: `Select-String -Path learn\ch1\notes\guides\*.md, learn\ch1\notes\research\*.md -Pattern 'model_tests|docs/cc_learn|--provider router|agentbook\.model_router|agentbook/model_router' | ForEach-Object { "$($_.Filename):$($_.LineNumber): $($_.Line.Trim())" }`

- [ ] **Step 11.2: 按映射替换（逐处人工确认语义）**

| 旧 | 新 |
|---|---|
| `model_tests/ch1/tX` | `learn/ch1/probes/tX` |
| `model_tests/infra/…` | `learn/infra/…` |
| `model_tests/evidence_variant/` | `learn/ch1/evidence/` |
| `docs/cc_learn/…` | `learn/ch1/notes/…` |
| `from agentbook.model_router import resolve` | `from learn.router import resolve` |
| `.venv\Scripts\python.exe chapter1/<exp>/main.py --provider router …` | `.venv\Scripts\python.exe learn\ch1\run.py --exp <exp> [-- 上游参数]`（对照 Task 7 契约的 args） |
| `… chapter1/learning-from-experience/run_experiment_8_2.py`（作者锚定 runner 的第一轨命令） | **不改**（第一轨 = 上游原样运行） |

- [ ] **Step 11.3: 指南头部加版本沿革注记**

在《模型路由与实验替换指南.md》标题后插入：

```markdown
> 沿革（2026-09-14）：路由层已迁至 `learn/router/`（原 `agentbook/model_router.py` 外挂），实验接线已改为运行适配层（`learn/ch1/contracts.py` + `learn/ch1/run.py`），上游文件恢复零修改。文中旧接线章节保留作为演进记录，现行运行方式以 `learn/README.md` 为准。
```

- [ ] **Step 11.4: 残留验证**

Run: `Select-String -Path learn\ch1\notes\**\*.md -Pattern 'model_tests|docs/cc_learn'`
Expected: 仅命中沿革注记与"历史记录"类表述，无活路径引用。

---

### Task 12: 验证阶梯（spec §八 全量执行）

- [ ] **Step 12.0 保真关**: `git diff origin/main..main --name-status` → 上游已有路径无 M/D（仅 A/R 且均属我们新增对象）
- [ ] **Step 12.1 import 关**: `.venv\Scripts\python.exe -c "import sys; sys.path.insert(0,'.'); from learn.router import resolve; print(resolve('text_only')['model'])"` → `glm-5.3-flash`
- [ ] **Step 12.2 冒烟关**: `.venv\Scripts\python.exe learn\infra\t10_router_smoke.py` → 7 路由与基线逐行一致
- [ ] **Step 12.3 探针关**: `.venv\Scripts\python.exe learn\ch1\probes\t2_zhipu_search.py` → 正常返回（小成本 1 次 API）
- [ ] **Step 12.4 适配层关**: `.venv\Scripts\python.exe learn\ch1\run.py --exp search-codegen --evidence` → 证据 json 落 `learn/ch1/evidence/`，exit_code=0
- [ ] **Step 12.5 第二轨关**: `.venv\Scripts\python.exe learn\infra\acceptance_router.py builtin` → 证据 json 落 `learn/ch1/evidence/`
- [ ] **Step 12.6 文档关**: `Select-String -Path learn\**\*.py -Pattern 'model_tests|from agentbook.model_router'` → 空；`git grep -n "provider router" -- chapter1` → 空
- [ ] **Step 12.7 密钥关**: `git log -p origin/main..main | Select-String -Pattern 'sk-[A-Za-z0-9]{8,}|[A-Za-z0-9]{32,}' | Select-Object -First 5` → 无真实 key 命中（命中需逐条人工判读是否误报）；`git ls-files | Select-String '^\.env$'` → 空

（12.4/12.5 涉及真实 API 成本；若额度受限，先跑 12.0-12.3+12.6-12.7，12.4/12.5 由学习者择机补跑。）

---

### Task 13: 人工复核交付包

- [ ] **Step 13.1: 变更全景**

Run: `git status --short; git diff origin/main..main --name-status`
记录输出，确认：所有 R/A 均属预期清单（Task 1/2/7/8/9 的对象），上游路径零 M/D。

- [ ] **Step 13.2: 复核清单（写入最终回复）**

1. `learn/` 目录树（学习产物 + 适配层）
2. 上游还原的 6 文件 diff（`git diff main -- chapter1 .gitignore` 应为空）
3. contracts.py / run.py 代码
4. 验证阶梯 12.0-12.7 输出
5. AGENTS.md / 方案文档勘误对照

- [ ] **Step 13.3: 停止，等待学习者复核后明确下令提交**

提示语（写入最终回复）："复核无误后，请明确说『提交』，我再按 spec §七 的提交序列执行 `git commit`；push 需另行确认。"

---

## Self-Review 记录（已执行）

1. **Spec 覆盖**：spec §六 6.1→Task 1/3；6.2→Task 6；6.3→Task 2/7/8/9；6.4→Task 2/4/5；6.5→Task 4/5/10/11；§七→Task 0-13（提交冻结）；§八→Task 12；§九/十（发布）不在本计划（push 由学习者另行确认后执行）。
2. **占位符扫描**：无 TBD/TODO；CT-1/CT-2 已在计划前置核实完毕，契约代码为最终形态。
3. **类型/命名一致性**：`learn.router.resolve`（Task 2/3/7/8 一致）；`contracts.apply_env / default_args / experiments / _CONTRACTS`（Task 5/7/8 一致）；`learn/ch1/variants/web-search-agent`（Task 1/3/5 一致）。
