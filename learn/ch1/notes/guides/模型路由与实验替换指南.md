# 模型路由与实验替换指南（最终版）

> 运行方式：`.venv\Scripts\python.exe`（或激活 .venv 后 `python`）。证据档案：[模型能力核对报告.md](../research/模型能力核对报告.md)（封存）。
>
> **沿革（2026-09-14）**：路由层已迁至 `learn/router/`（原 `agentbook/` 实为上游共享包，外挂文件已移出）；实验接线已改为运行适配层（`learn/ch1/contracts.py` 契约注入 + `learn/ch1/run.py` 统一启动），**上游文件恢复零修改**。现行运行方式以 [learn/README.md](../../../README.md) 为准；下文旧接线章节保留作为演进记录。
>
> 架构（现行）：`.env` 只放 key → `learn/router/model_config.json` 管连接/能力目录/任务路由 → `learn/router/model_router.py` 是代码唯一入口（`from learn.router import resolve`）。**实验代码里不允许出现厂商名、URL、key。命名规范：配置字段前缀 = 路由名大写（字段名回答"从哪来"，函数名回答"干什么"）；模型/厂商名禁止进入代码标识符。**

## 〇、判据总表

| 实验（内容） | 路由 | requires | 原模型 | 现配置 | 验收 |
|---|---|---|---|---|---|
| 1-1 上下文消融 | —（CLI 即路由） | text | registry 默认 glm-5.3 | zhipu/glm-5.3-flash | 消融三件套产出 |
| 1-2 联网搜索 ReAct | inline_search | web_search_inline | kimi-k3 + Moonshot Formula | zhipu/glm-5.3-flash（内联） | 实时答案 + 顶层 web_search 引用非空 |
| 1-3 Deep Research（托管搜索+沙箱回执） | deep_research | responses_api + hosted_web_search + hosted_code_interpreter | qwen3.7-plus（百炼） | dashscope/qwen3.7-plus（**唯一满足者**，已接路由） | result.json 含两种回执 + 316.35 km |
| 1-4 改写节点 | text_only | text | kimi-k3 | zhipu/glm-5.3-flash | calls 里 provider=zhipu, model=glm-5.3-flash |
| 1-4 生图（workflow 路线，异步任务式） | image_workflow | image_gen | wan2.2-t2i-flash（DashScope） | dashscope/wan2.2-t2i-flash（T12 实测） | calls 三段式记录，PNG 落盘 |
| 1-4 生图（native_gptimage 路线，同步式） | image_native | image_gen | gpt-image-2 | agnes/agnes-image-2.0-flash（T4/T12 时代 API 同构） | 出图 |
| 7-1 Q-learning | —（离线） | — | 无 LLM | 不变 | 原命令即过 |
| 7-2 LLM 臂 | text_only（`LLM_PROVIDER=router`） | text | kimi-k3（写死） | zhipu/glm-5.3-flash（router 分支） | 一局决策正常打印 |

## 一、路由层（已建成，位于 `agentbook/`）

### Step 1 · 根 `.env` 的 key 清单

```env
ZHIPU_API_KEY=...      # 配置于根 .env
AGNES_API_KEY=...      # 配置于根 .env
MIMO_API_KEY=...       # 配置于根 .env
DASHSCOPE_API_KEY=...  # 配置于根 .env
DEEPSEEK_API_KEY=...   # 可选备胎
```

### Step 2 · `agentbook/model_config.json`（实际生效内容）

```json
{
  "providers": {
    "zhipu":     { "base_url": "https://open.bigmodel.cn/api/paas/v4", "api_key_env": "ZHIPU_API_KEY" },
    "agnes":     { "base_url": "https://apihub.agnes-ai.com/v1", "api_key_env": "AGNES_API_KEY" },
    "mimo":      { "base_url": "https://api.xiaomimimo.com/v1", "api_key_env": "MIMO_API_KEY" },
    "deepseek":  { "base_url": "https://api.deepseek.com", "api_key_env": "DEEPSEEK_API_KEY" },
    "dashscope": { "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", "api_key_env": "DASHSCOPE_API_KEY" }
  },
  "models": {
    "zhipu/glm-5.3-flash":         { "caps": ["text", "multimodal", "web_search_inline"] },
    "zhipu/glm-5.3":               { "caps": ["text", "web_search_inline"] },
    "zhipu/glm-image":             { "caps": ["image_gen"], "notes": "未实测" },
    "agnes/agnes-3.0-flash":       { "caps": ["text"], "notes": "免费 20 RPM；T3 实测" },
    "agnes/agnes-image-2.0-flash": { "caps": ["image_gen"], "notes": "T4 实测通过" },
    "agnes/agnes-video-2.5-flash": { "caps": ["video_gen"], "notes": "未实测" },
    "mimo/mimo-v2.5-pro":          { "caps": ["text", "web_search_inline"], "notes": "T1 实测；联网插件需开通" },
    "deepseek/deepseek-v4-flash":  { "caps": ["text"] },
    "dashscope/wan2.2-t2i-flash":  { "caps": ["image_gen"], "notes": "异步任务式；T12 实测" },
    "dashscope/qwen3.7-plus":      { "caps": ["text", "responses_api", "hosted_web_search", "hosted_code_interpreter"], "notes": "唯一托管回执栈" }
  },
  "routes": {
    "text_only":     { "model": "zhipu/glm-5.3-flash", "requires": ["text"] },
    "multimodal":    { "model": "zhipu/glm-5.3-flash", "requires": ["multimodal"] },
    "inline_search": { "model": "zhipu/glm-5.3-flash", "requires": ["web_search_inline"],
                       "tools": [ { "type": "web_search", "web_search": { "enable": "True", "search_engine": "search_pro", "search_result": "True" } } ] },
    "image_workflow": { "model": "dashscope/wan2.2-t2i-flash", "requires": ["image_gen"],
                        "base_url": "https://dashscope.aliyuncs.com/api/v1",
                        "params": { "size": "1024*1024" } },
    "image_native":  { "model": "agnes/agnes-image-2.0-flash", "requires": ["image_gen"] },
    "web_search_api": { "model": "zhipu/glm-5.3-flash", "requires": ["text"] },
    "deep_research": { "model": "dashscope/qwen3.7-plus", "requires": ["responses_api", "hosted_web_search", "hosted_code_interpreter"] }
  }
}
```

要点：`provider/model` 键名是模型唯一身份；`image_workflow` 用 route 级 `base_url` 覆盖（wanx 走 DashScope 原生 `api/v1`，与 1-3 的 `compatible-mode/v1` 互不干扰）。

### Step 3 · 路由层（现位于 `learn/router/model_router.py`）

```python
"""任务模型路由：model_config.json 的唯一读取入口。

resolve(task) -> {"provider", "base_url", "api_key", "model", "tools"}
校验：route.requires 必须被 model.caps 满足；key 缺失即报错。
"""
import json
import os
from pathlib import Path

_CFG = json.loads(
    (Path(__file__).resolve().parent / "model_config.json").read_text(encoding="utf-8")
)


class RouterError(RuntimeError):
    pass


def resolve(task: str) -> dict:
    route = _CFG["routes"].get(task)
    if route is None:
        raise RouterError(f"未知任务路由 {task!r}，可选：{sorted(_CFG['routes'])}")
    entry = _CFG["models"].get(route["model"])
    if entry is None:
        raise RouterError(f"路由 {task!r} 的模型 {route['model']!r} 未登记在 models 目录")
    missing = sorted(set(route.get("requires", [])) - set(entry.get("caps", [])))
    if missing:
        raise RouterError(f"{route['model']} 缺少 {task} 所需能力 {missing}，caps={entry.get('caps')}")
    provider_id, model_id = route["model"].split("/", 1)
    prov = _CFG["providers"].get(provider_id)
    if prov is None:
        raise RouterError(f"{route['model']} 的厂商 {provider_id!r} 未定义")
    key = os.getenv(prov["api_key_env"], "")
    if not key:
        raise RouterError(f"缺少 {prov['api_key_env']}（写入根 .env）")
    return {
        "provider": provider_id,
        "base_url": route.get("base_url") or prov["base_url"],
        "api_key": key,
        "model": model_id,
        "tools": route.get("tools"),
        "params": route.get("params") or {},
    }
```

### Step 4 · 冒烟与探测（已全部实测通过）

```powershell
.venv\Scripts\python.exe learn\infra\t10_router_smoke.py   # 七条路由全绿
.venv\Scripts\python.exe learn\ch1\probes\t11_dashscope_probe.py  # deep_research 真实连通（qwen3.7-plus）
.venv\Scripts\python.exe learn\ch1\probes\t12_wanx_probe.py     # wanx 异步三段式全流程（1MB PNG）
```

## 二、逐实验接线状态（✅ = 已完成并验证）

### 2.1 实验 1-1（零改动，CLI 即路由）

```powershell
cd chapter1\context
python main.py --provider zhipu --model glm-5.3-flash --mode ablation
```powershell
cd <仓库根>   # 本机示例：E:\Documents\ai-agent-book
.venv\Scripts\python.exe learn\ch1\run.py --exp context -- --mode ablation
```

（现行方式：`learn/ch1/contracts.py` 的 context 契约注入 `ZHIPU_API_KEY/ZHIPU_BASE_URL/MODEL_NAME` 并默认透传 `--provider <路由provider> --model <路由model>`，上游 `main.py` 原样运行。下文 `--provider router` 命令为 2026-09 接线式历史方案，上游文件已还原。）

### 2.2 实验 1-2 ✅【建】`main_route.py`（`chapter1\web-search-agent\`）

一个入口两种模式：`--mode builtin`（内联快路径，消费 `inline_search` 路由）与 `--mode react`（ReAct 轨迹，脑=`text_only`、执行器=`web_search_api`；T5 实测可用）。原 `main.py`/`agent.py`（kimi + Formula）为验收锚定，一字未动。

```python
"""1-2 路由版主入口：--mode builtin（内联快路径）或 react（ReAct 轨迹）。

builtin: 单次调用，消费 inline_search 路由（服务端内联搜索，引用在顶层 web_search 字段）
react:   多轮编排，脑=text_only 路由、执行器=web_search_api 路由（客户端 ReAct，轨迹可见）
原 main.py / agent.py（kimi + Moonshot Formula）为验收锚定，一字未动。
"""
import argparse
import json
import time

import requests
from dotenv import load_dotenv
from openai import OpenAI

from learn.router import resolve

load_dotenv()

SYSTEM = (
    "你是联网搜索助手。先思考需要什么信息，再调用 web_search 工具，"
    "可多轮搜索；信息足够后综合给出带来源编号的答案。"
)
TOOLS = [{
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "联网搜索，返回网页标题、摘要与链接列表",
        "parameters": {
            "type": "object",
            "properties": {"query": {"type": "string", "description": "搜索关键词"}},
            "required": ["query"],
        },
    },
}]


def run_builtin(question: str, timeout: float) -> dict:
    """内联快路径：模型与搜索工具全部来自 inline_search 路由。"""
    cfg = resolve("inline_search")
    client = OpenAI(api_key=cfg["api_key"], base_url=cfg["base_url"], timeout=timeout)
    started = time.time()
    resp = client.chat.completions.create(
        model=cfg["model"],
        messages=[{"role": "user", "content": question}],
        tools=cfg["tools"],
    )
    dump = json.loads(json.dumps(resp.model_dump(), ensure_ascii=False, default=str))
    return {
        "mode": "builtin",
        "route": "inline_search",
        "provider": cfg["provider"],
        "model": cfg["model"],
        "question": question,
        "answer": resp.choices[0].message.content or "",
        "citations": dump.get("web_search") or [],
        "latency_s": round(time.time() - started, 1),
    }


def do_search(query: str, timeout: float) -> str:
    """执行器：web_search_api 路由（智谱独立搜索端点，T5 实测）。"""
    search = resolve("web_search_api")
    r = requests.post(
        f"{search['base_url']}/web_search",
        headers={"Authorization": f"Bearer {search['api_key']}"},
        json={"search_query": query, "search_engine": "search_pro_quark", "count": 5},
        timeout=timeout,
    )
    r.raise_for_status()
    items = r.json().get("search_result", [])
    if not items:
        return "(无结果)"
    return "\n".join(
        f"{i + 1}. {it.get('title', '')} | {it.get('link', '')}\n   {it.get('content', '')[:200]}"
        for i, it in enumerate(items)
    )


def run_react(question: str, max_rounds: int, timeout: float) -> dict:
    """轨迹版：脑=text_only 路由、执行器=web_search_api 路由（客户端 ReAct）。"""
    brain = resolve("text_only")
    client = OpenAI(api_key=brain["api_key"], base_url=brain["base_url"], timeout=timeout)
    messages = [{"role": "system", "content": SYSTEM}, {"role": "user", "content": question}]
    trace = []
    started = time.time()
    for rnd in range(1, max_rounds + 1):
        resp = client.chat.completions.create(model=brain["model"], messages=messages, tools=TOOLS)
        choice = resp.choices[0]
        reasoning = getattr(choice.message, "reasoning_content", None)
        if reasoning:
            print(f"💭 [{rnd}] {str(reasoning)[:300]}")
        if choice.finish_reason != "tool_calls":
            return {
                "mode": "react",
                "route": "text_only + web_search_api",
                "provider": brain["provider"],
                "model": brain["model"],
                "question": question,
                "rounds": trace,
                "answer": choice.message.content or "",
                "latency_s": round(time.time() - started, 1),
            }
        messages.append({
            "role": "assistant",
            "content": choice.message.content or "",
            "tool_calls": [
                {"id": tc.id, "type": "function",
                 "function": {"name": tc.function.name, "arguments": tc.function.arguments}}
                for tc in choice.message.tool_calls
            ],
        })
        round_entry = {"round": rnd, "reasoning_head": str(reasoning or "")[:200], "tool_calls": []}
        for tc in choice.message.tool_calls:
            call_args = json.loads(tc.function.arguments or "{}")
            query = call_args.get("query", "")
            print(f"🔧 [{rnd}] web_search(query={query})")
            result = do_search(query, timeout)
            print(f"👀 [{rnd}] {result[:200]}...")
            round_entry["tool_calls"].append({"query": query, "result_head": result[:300]})
            messages.append({"role": "tool", "tool_call_id": tc.id, "content": result})
        trace.append(round_entry)
    return {
        "mode": "react",
        "route": "text_only + web_search_api",
        "provider": brain["provider"],
        "model": brain["model"],
        "question": question,
        "rounds": trace,
        "answer": "(达到最大轮数，未得到最终答案)",
        "latency_s": round(time.time() - started, 1),
    }


def main():
    p = argparse.ArgumentParser(description="1-2 路由版（builtin=内联快路径 / react=ReAct 轨迹）")
    p.add_argument("question")
    p.add_argument("--mode", choices=["builtin", "react"], default="builtin")
    p.add_argument("--max-rounds", type=int, default=5)
    p.add_argument("--timeout", type=float, default=180.0)
    a = p.parse_args()
    if a.mode == "builtin":
        r = run_builtin(a.question, a.timeout)
    else:
        r = run_react(a.question, a.max_rounds, a.timeout)
    print(f"\n✅ 答案:\n{r['answer']}")
    if r["mode"] == "builtin":
        print(f"\n👀 来源（顶层 web_search，共 {len(r['citations'])} 条）:")
        print(json.dumps(r["citations"], ensure_ascii=False, indent=2)[:1500])


if __name__ == "__main__":
    main()
```

要点：`run_builtin` / `run_react` 返回结构化 dict（provider/model/answer/citations/trace），供第二轨验收 `learn/infra/acceptance_router.py` 直接采集；第二轨证据写 `learn/ch1/evidence/`，与作者基线（`chapter1/**/validation/`）分轨、永不混同。

### 2.3 实验 1-4 ✅【改】任务名化完成

【历史·已还原，现行走契约注入】**config.py 现状**（接线头一行化；三个节点全部走路由）：

```python
from learn.router import resolve

class Config:
    # ---- 改写节点：任务路由 ----
    _text_only = resolve("text_only")
    TEXT_ONLY_PROVIDER: str = _text_only["provider"]
    TEXT_ONLY_API_KEY: str = _text_only["api_key"]
    TEXT_ONLY_BASE_URL: str = _text_only["base_url"]
    TEXT_ONLY_MODEL: str = _text_only["model"]

    # ---- 工作流路线生图：任务路由（异步任务式 API，base_url 由路由覆盖）----
    _image_workflow = resolve("image_workflow")
    IMAGE_WORKFLOW_PROVIDER: str = _image_workflow["provider"]
    IMAGE_WORKFLOW_API_KEY: str = _image_workflow["api_key"]
    IMAGE_WORKFLOW_BASE_URL: str = _image_workflow["base_url"]
    IMAGE_WORKFLOW_MODEL: str = _image_workflow["model"]
    IMAGE_WORKFLOW_SIZE: str = _image_workflow["params"].get("size", "1024*1024")

    # ---- 生图节点（原生路线）：任务路由 ----
    _image_native = resolve("image_native")
    IMAGE_NATIVE_PROVIDER: str = _image_native["provider"]
    IMAGE_NATIVE_API_KEY: str = _image_native["api_key"]
    IMAGE_NATIVE_BASE_URL: str = _image_native["base_url"]
    IMAGE_NATIVE_MODEL: str = _image_native["model"]
```

`required_env` 收窄为 `["TEXT_ONLY_API_KEY", "IMAGE_NATIVE_API_KEY"]`；DASHSCOPE_/SILICONFLOW_/GEMINI_ 旧字段已随路由化清理（wanx/gemini 函数体保留、gemini 无 key 为摆设）。

**pipeline.py**：改写/工作流生图/原生生图三个函数的凭据、端点、模型、尺寸全部改读 `Config.TEXT_ONLY_*` / `Config.IMAGE_WORKFLOW_*` / `Config.IMAGE_NATIVE_*`（前缀=路由名）；workflow 路线保持 `generate_image_wanx(prompt, negative_prompt)`（异步三段式 + 负面词工作流原样保留）；`native_gptimage` 路线走 `generate_image_gpt_image`（Agnes）；call record 的 provider 标签改从路由取值。

### 2.4 实验 7-2 ✅【改】

【历史·已还原，现行走契约注入】`llm_agent.py`：接线头 `from learn.router import resolve` + provider 链新增 `router` 分支（原 moonshot/dashscope 分支未动）。运行：

```powershell
$env:LLM_PROVIDER="router"; python experiment.py --mode llm --llm-episodes 1
```

边界：正式验收 `run_experiment_8_2.py` 走原 provider，不受影响。

### 2.5 实验 1-3 ✅【改】已接路由

[search-codegen/config.py](../../../chapter1/search-codegen/config.py) 的 dashscope 分支改为：

```python
        if backend == "dashscope":
            from learn.router import resolve

            cfg = resolve("deep_research")
            return cfg["api_key"], cfg["base_url"], model or cfg["model"]
```

连接参数（国内站 base_url + qwen3.7-plus）现在只由 `model_config.json` 决定，`.env` 里的 `DASHSCOPE_BASE_URL`/`DASHSCOPE_MODEL` 不再被读取（`DASHSCOPE_API_KEY` 仍是 key 来源）。

**计费与作者实证**：原作者验收用的就是 DashScope 国际站按量（validation/runs/real_20260731T170529Z/evidence.json 记录 base_url=dashscope-intl…；receipts 响应头 `x_billing_type: "response_api"`、`x_tools` 逐次计数托管工具用量）。托管工具在按量下直接可用：code_interpreter 免费（[插件概述](https://help.aliyun.com/zh/model-studio/plug-in-overview)）、web_search 按千次计费（[Web search 文档](https://www.alibabacloud.com/help/en/model-studio/web-search)）。官方文档列明该托管栈还支持 deepseek-v4-flash / v4-pro / glm-5.2——想变体验证可 `--model deepseek-v4-flash`（未实测，单独标注，不与作者基线混同）。

跑验收：

```powershell
cd chapter1\search-codegen
python main.py --backend dashscope --mode single --request "东盟 10 国首都之间最近的一对是哪两个？请搜索并用 Python 计算" --output result.json
```

## 三、执行状态（✅ = AI 已完成；□ = 你本人跑）

- [x] 1. 路由层三件套落盘 `agentbook/` + 冒烟
- [x] 2. 1-2 两个脚本 + T5/T10/T11 验证
- [x] 3. 1-4 任务名化 + workflow/native 双路线恢复 + T12
- [x] 4. 7-2 router 分支
- [x] 5. 1-3 接路由
- [ ] 6. 你：V1 消融（2.1 命令）
- [ ] 7. 你：V2 `python main_route.py "比特币现价" --mode builtin`
- [ ] 8. 你：V3 `python main.py --route native_gptimage --requirement agi-programmer`
- [ ] 9. 你：V4 `python main.py --route workflow --requirement windowsill-plant`（改写+wanx 全链路）
- [ ] 10. 你：V5 `$env:LLM_PROVIDER="router"; python experiment.py --mode llm --llm-episodes 1`
- [ ] 11. 你：V6 1-3 东盟验收（2.5 命令）
- [ ] 12. 你（可选）：第二轨变体验收 `.venv\Scripts\python.exe learn\infra\acceptance_router.py all`
- [ ] 13. 你：人工 diff 审阅 → 决定是否 commit

## 四、验收标准速查

| # | 通过标准 |
|---|---|
| V0 | T10 七条路由全绿（已达成 ✅） |
| V1 | 消融三件套产出，无 401/模型不存在 |
| V2 | 实时价格 + 顶层 web_search 引用非空 |
| V3 | calls 里 provider=agnes, model=agnes-image-2.0-flash，出图 |
| V4 | 改写节点 provider=zhipu；生图节点三段式 calls（provider=dashscope, model=wan2.2-t2i-flash），PNG 落盘 |
| V5 | 一局 LLM 决策正常打印 |
| V6 | result.json 含 `web_search_call`+`code_interpreter_call`，东盟 316.35 km |

## 五、测试存档（T1–T12，均已实测；脚本位于 `learn/ch1/probes/` 与 `learn/infra/`，目录约定见 `learn/README.md`）

| # | 结论 |
|---|---|
| T2 ✅ | 智谱 paas/v4：glm-5.3/flash 均支持内联 web_search；引用在顶层 `web_search` 字段 |
| T1 ✅ | MiMo chat 内联 web_search 可用（`annotations.url_citation`）——备胎 |
| T3 ✅ | Agnes key 归属 apihub.agnes-ai.com；文本可对话 |
| T4 ✅ | agnes-image-2.0-flash 生图成功（`data[0].url`） |
| T5 ✅ | 智谱独立搜索 API 可用（HTTP 200 + search_result 3 条） |
| T6 ❌ | 智谱 `/api/v1/responses` 仅 Coding Plan 可用（按量 429） |
| T7 ❌ | Agnes `/responses` 有协议无托管执行 |
| T8 ❌ | MiMo `/responses` 网关不支持托管工具 |
| T9 ❌ | Agnes chat 无任何托管工具；zhipu chat 无 code_interpreter |
| T10 ✅ | 路由冒烟：七条路由全绿 |
| T11 ✅ | DashScope 按量 key 探测：qwen3.7-plus 连通 |
| T12 ✅ | wanx 异步三段式全流程：1MB PNG，3 段 calls |

## 六、边界

1. 验收 runner（`run_experiment_1_2.py`、`run_experiment_8_2.py`、1-3 验证器）的代码路径未接入路由——锚定=证据可比性+防伪+1-3 的托管栈本身就是实验对象；路由探索不构成假验收。
2. `.env` 永不入库；`agentbook/model_config.json` / `model_router.py` 无密钥，随包入库。
3. 行号 ±3 行以内按内容定位；冲突时以本地代码 + 实测为裁判。
