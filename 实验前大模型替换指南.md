# 实验前大模型替换指南（第 1 章）

> 配套文档：[ch1实验核对报告.md](ch1实验核对报告.md)（所有结论的证据与验证路径）
> 你的 Key：智谱（官方）、DeepSeek（官方）、MiMo（官方）、Agnes（官方）、火山 codeplan 生图
> **定稿（2026-09-13）**：文本统一 **glm-5.3-flash**（1-1 / 1-2 / 1-4 改写 / 7-2），生图 **agnes-image**，1-3 先 T6 实测智谱托管工具（通过则同为 flash）—— 见 §0.1 与 §5.1
> 原则：**先测试（T）→ 再替换（R）→ 后验证（V）**；所有改动本地保留，不提交回上游仓库

---

## 0. 替换总览（做什么、动哪里、多大代价）

| 实验 · 节点 | 原模型 | 换成（定稿） | 改动方式 | 代价 |
|---|---|---|---|---|
| 1-1 对话 LLM | 任意 provider | **glm-5.3-flash** | 命令行 `--model glm-5.3-flash`（registry 零改动） | ⭐ 零改动 |
| 1-2 搜索 Agent | kimi-k3 + Moonshot Formula | **方案 C**：glm-5.3-flash 脑 + 智谱搜索 API（deepseek 备选脑）；快路径：智谱内联（T2 测 flash） | 新建独立脚本，不动原文件 | ⭐⭐⭐ / ⭐⭐ |
| 1-4 改写节点 | kimi-k3 | **glm-5.3-flash** | `.env` 三行 | ⭐ 零代码 |
| 1-4 生图（原生对照路线） | gpt-image-2 | **agnes-image-2.0-flash** | `.env` 三行（复用 native_gptimage 路线） | ⭐ 零代码 |
| 1-4 生图（工作流路线） | wan2.2-t2i-flash | agnes-image | pipeline.py 加一个函数 | ⭐⭐⭐ 可选（可跳过） |
| 7-2 LLM 臂 | kimi-k3 | **glm-5.3-flash**（deepseek 备选） | llm_agent.py 加 1 个分支 + experiment.py 扩 key 表 | ⭐⭐ |
| 1-3 Deep Research | qwen3.7-plus（百炼） | **先跑 T6 实测智谱托管工具**：通过 → glm-5.3-flash 直跑；不通 → 百炼原模型或无回执学习版 | T6 探针 / 只加 env | ⭐ 十分钟（见 §5.1） |

**一个重要教学点（先读懂再动手）**：1-2 的搜索机制要分**三档**看，不是"两段式 vs 内联"的二分：

| 档 | 编排权 | 搜索执行 | 代表 | 轨迹形态 |
|---|---|---|---|---|
| ① 客户端编排 | 你的代码（ReAct 循环） | 模型厂商托管端点（Formula Fiber） | k3 原版 | 💭→🔧→👀 **多轮可见** |
| ② 服务端内联 | 模型服务端 | 模型服务端 | MiMo / 智谱 chat-search | 单次调用，**轨迹退化** |
| ③ 客户端编排 + 任意搜索 API | 你的代码 | 你调的任何搜索服务 | 方案 C（R4 节） | 💭→🔧→👀 多轮可见 |

k3 原版的本质是 ①：模型只发 `tool_calls`（agent.py#L408 分支），**执行是你机器上的代码**发起的第二次 HTTP 调用（`_execute_formula` POST `/formulas/.../fibers`，agent.py#L232-L297），结果由客户端塞回对话（L453-L477）。正因为编排权在客户端，**任何支持 function calling 的模型 + 任意搜索 API 都能复现 1-2**（= 方案 C）；仓库在 agent.py#L56-L68 的 `search_impl` 注释里明说了这个扩展方向。而 ② 的内联模式把"行动→观察"内化到服务端、循环退化为单次调用——这与 1-4 里万相 `actual_prompt` 字段同构：**适配层正在被模型服务端内化**，正是书里让你观察的现象（但代价是看不到多轮搜索决策）。

### 0.1 定稿卡（2026-09-13）：glm-5.3-flash 一把梭 + Agnes 生图

**glm-5.3-flash 事实卡**（官方模型页 [docs.bigmodel.cn/cn/guide/models/vlm/glm-5.3-flash](https://docs.bigmodel.cn/cn/guide/models/vlm/glm-5.3-flash)）：
- Model Code 就是 `glm-5.3-flash`；**GLM-5 系列首个原生多模态模型**（输入：视频/图像/文本/文件；输出：文本），1M 上下文、128K 输出、支持 Function Calling
- 推荐参数：`temperature: 1`、`top_p: 0.95`、`reasoning_effort: max`、`thinking.type` 仅支持 `enabled`（不能关思考）；温度合法范围 (0.0, 1.0]
- 价格：输入 0.8 元/M、输出 2.8 元/M（限时五折 0.4/1.4），约为 glm-5.3 的 1/10
- **多模态对本章 5 个实验都用不上**（1-4 生图走独立节点，改写节点只收发文本），所以"flash 是否多模态"不阻塞任何替换，只是白赚的能力

**定稿带来的收敛**：
1. **1-1 零代码改动**：CLI 有 `--model`（[context/main.py#L1215-L1219](chapter1/context/main.py#L1215-L1219)），解析层显式 model 优先于 `default_model`（[resolution.py#L167-L169](agentbook/providers/resolution.py#L167-L169)）→ `--provider zhipu --model glm-5.3-flash` 直接跑，**R1 的 registry 改动不再需要**
2. **MiMo 全线退出关键路径**：T1、R1 的 mimo 条目、R4/R5 的 mimo 分支全部降级为可选；1-2 先试 flash 内联（T2），失败走方案 C（T5）
3. **`.env` 只需新增 AGNES_API_KEY 一个**（1-1/1-2/1-4 改写/7-2 全部复用已在用的 ZHIPU_API_KEY）
4. **测试最小集**：T2（只看 flash 行）→ T3 → T4 → T6（1-3 能否不用百炼的探针）；T5 条件触发

---

## 1. 准备：`.env` 追加变量（一次性）

在仓库根 `.env` 追加（`KEY=VALUE` 格式，别留裸行）：

```env
AGNES_API_KEY=你的agnes-key
# AGNES_BASE_URL 先不填，等 T3 测出归属站再填
# MIMO_API_KEY=...       （可选：定稿后 MiMo 退出关键路径，想留备选再填）
# DASHSCOPE_API_KEY=...  （可选：仅当要跑 1-3 完整验收时加，见 §5.1）
```

定稿后**必加的只有 AGNES_API_KEY 一行**——1-1 / 1-2 / 1-4 改写 / 7-2 全部复用已在用的 `ZHIPU_API_KEY`。

---

## 2. 第一步：能力实测（定稿优先级：T2 → T3 → T4 → T6；T5 条件触发；T1 可选）

在仓库根新建 `model_tests\` 目录，测试脚本按定稿优先级存进去（先 T2/T3/T4），**从仓库根运行**（`load_dotenv()` 会向上找到根 `.env`）。

### T1 · MiMo 联网搜索（决定 1-2 方案）

存为 `model_tests\t1_mimo_search.py`：

```python
import os, json
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()
client = OpenAI(
    api_key=os.environ["MIMO_API_KEY"],
    base_url="https://api.xiaomimimo.com/v1",
)
resp = client.chat.completions.create(
    model="mimo-v2.5-pro",
    messages=[{"role": "user", "content": "今天北京的天气怎么样？"}],
    tools=[{"type": "web_search", "max_keyword": 3, "force_search": True, "limit": 1}],
    extra_body={"thinking": {"type": "disabled"}},
)
msg = resp.choices[0].message
print("答案:", msg.content)
print("完整响应（找引用字段位置）:")
print(json.dumps(resp.model_dump(), ensure_ascii=False, indent=2)[:3000])
```

运行：`python model_tests\t1_mimo_search.py`
**判读**：答案包含今天日期/天气 + 响应里能找到 `url_citation` 类引用 → **通过**。
**失败排查**：401 → 检查 key；提示无 web_search 工具 → 去[控制台-插件管理](https://platform.xiaomimimo.com/#/console/plugin)开通"联网服务插件"（生效需几分钟）；若 OpenAI SDK 鉴权不通，改用 `requests` + 请求头 `"api-key": key`（官方 curl 用这个头）。

### T2 · 智谱 web_search（glm-5.3 与 flash 各测一次，决定 1-2 备选）

存为 `model_tests\t2_zhipu_search.py`：

```python
import os, json
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()
client = OpenAI(
    api_key=os.environ["ZHIPU_API_KEY"],
    base_url="https://open.bigmodel.cn/api/paas/v4",
)
for model in ("glm-5.3", "glm-5.3-flash"):
    print(f"\n===== {model} =====")
    try:
        resp = client.chat.completions.create(
            model=model,
            messages=[{"role": "user", "content": "今天北京的天气怎么样？"}],
            tools=[{"type": "web_search",
                    "web_search": {"enable": "True", "search_engine": "search_pro",
                                    "search_result": "True"}}],
        )
        print("答案:", resp.choices[0].message.content[:500])
        print("=> 支持内置搜索")
    except Exception as e:
        print(f"=> 不支持: {type(e).__name__}: {e}")
```

运行：`.venv\Scripts\python.exe model_tests\t2_zhipu_search.py`
**实测结论（2026-09-13）✅✅**：glm-5.3 与 glm-5.3-flash 在智谱自有平台（paas/v4）**都支持内联 web_search**（两者均返回当日北京天气实时内容）——百炼能力表的"flash 联网搜索：不支持"不适用于智谱自有端点。→ **1-2 快路径（方案 A）可直接用 flash**；方案 C 仍为轨迹保真正选。遗留小项：响应 `annotations` 为空，实施方案 A 时先 dump 完整响应定位智谱的引用字段位置。

### T3 · Agnes 站点归属（决定所有 Agnes 配置的 base_url）

存为 `model_tests\t3_agnes_site.py`：

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()
key = os.environ["AGNES_API_KEY"]
for base in ("https://apihub.agnes-ai.com/v1", "https://api.agnes-ai.cn/v1"):
    print(f"\n===== {base} =====")
    try:
        client = OpenAI(api_key=key, base_url=base, timeout=30)
        resp = client.chat.completions.create(
            model="agnes-3.0-flash",
            messages=[{"role": "user", "content": "回复两个字：成功"}],
        )
        print("回复:", resp.choices[0].message.content)
        print("=> 你的 key 归属此站")
    except Exception as e:
        print(f"=> 不通: {type(e).__name__}: {str(e)[:200]}")
```

**判读**：通的那个站填进 `.env`：`AGNES_BASE_URL=<通的那个>`。

### T4 · Agnes 文生图（决定 1-4 生图方案）

存为 `model_tests\t4_agnes_image.py`（把 `<BASE>` 换成 T3 的结果）：

```python
import os, requests
from dotenv import load_dotenv

load_dotenv()
base = os.getenv("AGNES_BASE_URL", "<BASE>")
r = requests.post(
    f"{base}/images/generations",
    headers={"Authorization": f"Bearer {os.environ['AGNES_API_KEY']}"},
    json={"model": "agnes-image-2.0-flash",
          "prompt": "a red apple on a white wooden table, soft daylight",
          "size": "1024x1024"},
    timeout=120,
)
print("HTTP", r.status_code)
body = r.json()
print(json.dumps(body, ensure_ascii=False)[:1500])
# 看返回里是 data[0].url 还是 b64_json —— R3 会用到
```

**判读**：HTTP 200 且含图片 url 或 b64 → **通过**；429 → 免费层 1K 有效 20 RPM，稍等重试。

### T5 · 智谱独立搜索 API（方案 C 的执行器，与模型解耦）

存为 `model_tests\t5_zhipu_search_api.py`：

```python
import os, json, requests
from dotenv import load_dotenv

load_dotenv()
r = requests.post(
    "https://open.bigmodel.cn/api/paas/v4/web_search",
    headers={"Authorization": f"Bearer {os.environ['ZHIPU_API_KEY']}"},
    json={"search_query": "DeepSeek V4 发布",
          "search_engine": "search_pro_quark", "count": 3},
    timeout=30,
)
print("HTTP", r.status_code)
print(json.dumps(r.json(), ensure_ascii=False)[:1200])
```

运行：`python model_tests\t5_zhipu_search_api.py`
**判读**：返回 `search_result` 数组（含 title/link/content）→ **通过**，方案 C 可用。
**注意**：这个端点是独立的搜索服务，**不依赖 T2 的结论**——即使 glm-5.3-flash 不支持内联搜索，方案 C 照样能跑（模型只管 function calling，搜索走这个端点）。

### T6 · 智谱 Responses 端点托管工具实测（1-3 能否不用百炼的关键）

存为 `model_tests\t6_zhipu_responses.py`：

```python
import os, json, requests
from dotenv import load_dotenv

load_dotenv()
r = requests.post(
    "https://open.bigmodel.cn/api/v1/responses",
    headers={"Authorization": f"Bearer {os.environ['ZHIPU_API_KEY']}"},
    json={
        "model": "glm-5.3-flash",
        "input": "请联网搜索：智谱 GLM-5.3-Flash 的发布日期是什么时候？",
        "tools": [{"type": "web_search", "search_context_size": "medium"}],
        "reasoning": {"effort": "low"},
    },
    timeout=120,
)
print("HTTP", r.status_code)
body = r.json()
print(json.dumps(body, ensure_ascii=False, indent=2)[:3000])
types = [it.get("type") for it in body.get("output", []) if isinstance(it, dict)]
print("\n输出项类型:", types)
print("有 web_search_call 回执:", "web_search_call" in types)
```

**判读（三种结局）**：
- HTTP 200 且输出项含 `web_search_call` → **智谱会代跑托管搜索** → 1-3 可全程 glm-5.3-flash + 智谱，百炼不用注册（再补测 `code_interpreter` 后按 §5.1 决策树跑完整实验）
- HTTP 200 但没有 `web_search_call`（模型直接口答）→ 工具声明被忽略，智谱只做推理 → 1-3 完整验收退回百炼，或接受"无回执学习版"
- 4xx/5xx → 端点不认 OpenAI 风格托管工具声明 → 同上

**实测记录（2026-09-13）⏸️**：两发探针均 **HTTP 429 `permission_denied`**："您的GLM Coding Plan套餐已到期，暂无法使用"。→ 这把 `ZHIPU_API_KEY` 被识别为 **Coding Plan key 且已到期**，而 `/api/v1` 是 Coding Plan 专属端点——T6 被**套餐状态**挡住，"智谱是否代跑托管工具"仍未判定（不是被否定）。待本人操作后重跑（三选一见 §5.1）。注意 paas/v4 不受影响（T2 两模型均通）。

**为什么这一发就够**：1-3 的代码**不锁模型也不锁平台**——`--backend dashscope` 只是第二配置槽（[config.py#L63-L64](chapter1/search-codegen/config.py#L63-L64)，key/base_url/model 全来自 env，`--model` 可覆盖）；验证器的模型身份检查是"请求==返回"的相对检查（[run_experiment_1_3.py#L120-L125](chapter1/search-codegen/run_experiment_1_3.py#L120-L125)），后端白名单按 flag 名记录（[#L51](chapter1/search-codegen/run_experiment_1_3.py#L51)）。唯一写死的是：响应里必须有平台代跑的 `web_search_call`/`code_interpreter_call` 回执（[#L137-L140](chapter1/search-codegen/run_experiment_1_3.py#L137-L140)）——**这是平台能力，T6 测的就是它**。

---

## 3. 第二步：替换步骤（R1–R5，测试通过后再做）

### R1 · 实验 1-1：零改动（⭐ 定稿后不需要改 registry）

定稿后 1-1 直接用命令行指定 flash，**不改动任何文件**：

```powershell
cd chapter1\context
python main.py --provider zhipu --model glm-5.3-flash --mode ablation
```

依据：CLI 有 `--model` 参数（[context/main.py#L1215-L1219](chapter1/context/main.py#L1215-L1219)）；`resolve_backend` 对显式传入的 model 直接优先（[resolution.py#L167-L169](agentbook/providers/resolution.py#L167-L169)），不走 `default_model`。

**（可选，定稿可跳过）**想给 1-1 多加 mimo / agnes provider 时，改 [agentbook/providers/registry.py](agentbook/providers/registry.py)，在 `PROVIDERS` 字典内（`"ollama"` 条目之后、结束花括号前）追加：

```python
    "mimo": Provider(
        name="mimo",
        base_url="https://api.xiaomimimo.com/v1",
        default_model="mimo-v2.5-pro",
        key_vars=("MIMO_API_KEY",),
    ),
    "agnes": Provider(
        name="agnes",
        base_url="https://apihub.agnes-ai.com/v1",  # T3 实测后改成归属站
        default_model="agnes-3.0-flash",
        key_vars=("AGNES_API_KEY",),
    ),
```

无需改任何 argparse——registry.py#L134-L142 的注释确认各章 `--provider` 选项由这张表自动生成。
**冒烟**：`cd chapter1\context` 后 `python main.py --provider mimo`（agnes 同理），出现对应 prompt 即通过。

### R2 · 实验 1-4 改写节点：换 glm-5.3-flash（⭐ 零代码）

`.env` 三行（放在 1-4 会读到的位置即可，根 .env 全实验共用）：

```env
KIMI_API_KEY=<你的ZHIPU_API_KEY>
KIMI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
REWRITE_MODEL=glm-5.3-flash
```

**为什么安全**：[pipeline.py#L119-L133](chapter1/image-gen-workflow/pipeline.py#L119-L133) 的改写调用**不传 temperature**（原注释写明是为规避 kimi-k3 只许 temperature=1 的限制），换任何 OpenAI 兼容模型都没有该风险。MiMo/Agnes 想当改写节点，同法换三行即可。
**注意坑**：[config.py#L63-L65](chapter1/image-gen-workflow/config.py#L63-L65) 的 `validate()`（main.py#L149 调用）要求 **KIMI/DASHSCOPE/GEMINI/OPENAI 四个变量全部非空**——不跑的路线填占位符（如 `GEMINI_API_KEY=placeholder`）。

### R3 · 实验 1-4 生图节点：Agnes

**方案 A（⭐ 零代码，先做这个）**：`native_gptimage` 路线走标准 `/images/generations`（[pipeline.py#L299-L340](chapter1/image-gen-workflow/pipeline.py#L299-L340)），与 Agnes 图片接口同构。`.env` 三行：

```env
OPENAI_BASE_URL=<T3 测出的 Agnes base_url>
OPENAI_API_KEY=<你的AGNES_API_KEY>
GPT_IMAGE_MODEL=agnes-image-2.0-flash
```

跑 `python main.py --route native_gptimage` → 该"原生路线"即由 Agnes 出图，与 R2 改造后的 workflow 路线形成完整对照。
**兼容性依据**：函数内 size 固定 `1024x1024`（Agnes 官方文档示例恰好支持），且响应处理同时兼容 `url` 与 `b64_json` 两种返回。

**方案 B（⭐⭐⭐ 可选进阶）**：想保留"改写 → Agnes 生图"的完整工作流对照，在 [pipeline.py](chapter1/image-gen-workflow/pipeline.py) 仿照 `generate_image_gpt_image` 新增 `generate_image_agnes(prompt, negative_prompt)`（改用 `Config.AGNES_*` 字段），再仿照 `run_workflow_route`（L348）新增 `run_workflow_agnes_route`，并在 `ROUTE_RUNNERS`（L404）注册 `"workflow_agnes"`。config.py 加对应 `AGNES_API_KEY/AGNES_BASE_URL/AGNES_IMAGE_MODEL` 读取。
**火山 codeplan 生图**：等你按核对报告 E2 验证其 API 形态后再决定是否加路线；接口形态未知前不动手。

### R4 · 实验 1-2：两条替换路线（都不动原文件）

按第 0 节的三档分类选路线：**要轨迹保真选方案 C，要最快出结果选方案 A**。

#### 方案 C · 客户端编排 ReAct（⭐⭐⭐ 首选：轨迹与原版同构，模型/引擎双解耦）

原理：模型只负责发 `tool_calls`（glm-5.3-flash 官方支持 Function Calling；deepseek 备选脑），**搜索执行由你的代码调智谱独立搜索 API**（T5 通过后即用）。这就是 k3 Formula 的同构替换：编排仍在客户端，只是把"Moonshot 托管 Fiber"换成"你的搜索函数"。

在 `chapter1\web-search-agent\` 新建 `main_react_search.py`：

```python
"""1-2 客户端编排 ReAct 版：任意 FC 模型 + 智谱独立搜索 API。
轨迹形态与原版 agent.py（Formula）同构；原文件保留不动。"""
import argparse, json, os
import requests
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

SEARCH_URL = "https://open.bigmodel.cn/api/paas/v4/web_search"
MODELS = {
    "zhipu": ("https://open.bigmodel.cn/api/paas/v4", "ZHIPU_API_KEY", "glm-5.3-flash"),
    "deepseek": ("https://api.deepseek.com", "DEEPSEEK_API_KEY", "deepseek-v4-flash"),
    # "mimo": ("https://api.xiaomimimo.com/v1", "MIMO_API_KEY", "mimo-v2.5-pro"),  # 定稿后可选
}
TOOLS = [{
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "联网搜索，返回网页标题、摘要与链接列表",
        "parameters": {"type": "object",
                        "properties": {"query": {"type": "string",
                                                  "description": "搜索关键词"}},
                        "required": ["query"]},
    },
}]
SYSTEM = ("你是联网搜索助手。先思考需要什么信息，再调用 web_search 工具，"
          "可多轮搜索；信息足够后综合给出带来源编号的答案。")

def do_search(query: str) -> str:
    r = requests.post(SEARCH_URL,
        headers={"Authorization": f"Bearer {os.environ['ZHIPU_API_KEY']}"},
        json={"search_query": query, "search_engine": "search_pro_quark",
              "count": 5}, timeout=30)
    r.raise_for_status()
    items = r.json().get("search_result", [])
    if not items:
        return "(无结果)"
    return "\n".join(f"{i+1}. {it.get('title','')} | {it.get('link','')}\n"
                     f"   {it.get('content','')[:200]}"
                     for i, it in enumerate(items))

def run(model_key: str, question: str, max_rounds: int, timeout: float):
    base_url, key_env, model = MODELS[model_key]
    client = OpenAI(api_key=os.environ[key_env], base_url=base_url, timeout=timeout)
    messages = [{"role": "system", "content": SYSTEM},
                {"role": "user", "content": question}]
    for rnd in range(1, max_rounds + 1):
        resp = client.chat.completions.create(model=model, messages=messages,
                                               tools=TOOLS)
        choice = resp.choices[0]
        msg = choice.message
        reasoning = getattr(msg, "reasoning_content", None)
        if reasoning:
            print(f"💭 [{rnd}] {str(reasoning)[:300]}")
        if choice.finish_reason != "tool_calls":
            print(f"\n✅ 最终答案:\n{msg.content or '(空)'}")
            return
        messages.append({"role": "assistant", "content": msg.content or "",
                         "tool_calls": [{"id": tc.id, "type": "function",
                                          "function": {"name": tc.function.name,
                                                       "arguments": tc.function.arguments}}
                                         for tc in msg.tool_calls]})
        for tc in msg.tool_calls:
            args = json.loads(tc.function.arguments or "{}")
            print(f"🔧 [{rnd}] web_search(query={args.get('query','')})")
            result = do_search(args.get("query", ""))
            print(f"👀 [{rnd}] {result[:200]}...")
            messages.append({"role": "tool", "tool_call_id": tc.id,
                             "content": result})
    print("(达到最大轮数，未得到最终答案)")

if __name__ == "__main__":
    p = argparse.ArgumentParser(description="1-2 客户端编排 ReAct 版")
    p.add_argument("question")
    p.add_argument("--model", choices=list(MODELS), default="zhipu")
    p.add_argument("--max-rounds", type=int, default=5)
    p.add_argument("--timeout", type=float, default=120.0)
    a = p.parse_args()
    run(a.model, a.question, a.max_rounds, a.timeout)
```

运行（在 `chapter1\web-search-agent\` 下）：
```powershell
python main_react_search.py "2024年诺贝尔物理学奖获得者是谁？" --model zhipu
python main_react_search.py "2024年诺贝尔物理学奖获得者是谁？" --model deepseek
```
**通过标准**：终端能看到多轮 🔧 搜索 / 👀 观察，最终答案带来源编号——与原版 ReAct 轨迹同构。**学习点**：对比原版 agent.py 会发现你写的循环和它的 `search_and_answer`（L364-L520）结构几乎一样，唯一区别是工具执行器从 `_execute_formula` 换成了 `do_search`——这就是"编排权在客户端"的实证。

#### 方案 A · 服务端内联快速版（⭐⭐ 改动最小，轨迹退化为单次调用）

适用：想最快拿到"能联网的答案"，或专门观察"内联后多轮轨迹消失"这一现象。

在 `chapter1\web-search-agent\` 新建 `main_builtin_search.py`：

```python
"""1-2 的内置搜索版：MiMo / 智谱 web_search（服务端内联，单次调用）。
原 agent.py（Moonshot Formula 两段式）保留不动，便于两种机制对照。"""
import argparse, json, os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

BACKENDS = {
    "mimo": {
        "base_url": "https://api.xiaomimimo.com/v1",
        "key_env": "MIMO_API_KEY",
        "model": "mimo-v2.5-pro",
        "tools": [{"type": "web_search", "max_keyword": 3, "limit": 1}],
        "extra_body": {"thinking": {"type": "disabled"}},
    },
    "zhipu": {
        "base_url": "https://open.bigmodel.cn/api/paas/v4",
        "key_env": "ZHIPU_API_KEY",
        "model": "glm-5.3-flash",  # 以 T2 实测为准：flash 报不支持则改回 glm-5.3 或走方案 C
        "tools": [{"type": "web_search",
                   "web_search": {"enable": "True", "search_engine": "search_pro",
                                   "search_result": "True"}}],
        "extra_body": {},
    },
}

SYSTEM = ("你是联网搜索助手。需要实时信息时使用 web_search 工具，"
          "综合结果后给出结构清晰、带来源的答案。")

def run(backend: str, question: str, timeout: float):
    cfg = BACKENDS[backend]
    key = os.getenv(cfg["key_env"], "")
    if not key:
        raise SystemExit(f"未设置 {cfg['key_env']}")
    client = OpenAI(api_key=key, base_url=cfg["base_url"], timeout=timeout)
    kwargs = dict(model=cfg["model"],
                  messages=[{"role": "system", "content": SYSTEM},
                            {"role": "user", "content": question}],
                  tools=cfg["tools"])
    if cfg["extra_body"]:
        kwargs["extra_body"] = cfg["extra_body"]
    print(f"💭 [{backend}/{cfg['model']}] 服务端搜索中（内联模式，可能数十秒）...")
    resp = client.chat.completions.create(**kwargs)
    msg = resp.choices[0].message
    print("\n✅ 最终答案:\n" + (msg.content or "(空)"))
    dump = json.loads(json.dumps(resp.model_dump(), ensure_ascii=False, default=str))
    cites = dump["choices"][0]["message"].get("annotations") or []
    if cites:
        print("\n👀 来源:")
        for c in cites:
            c = c.get("url_citation", c)
            print(f"  - {c.get('title', '')}: {c.get('url', '')}")
    else:
        print("\n👀 未解析到 annotations；请检查完整响应确定引用字段位置：")
        print(json.dumps(dump, ensure_ascii=False, indent=2)[:4000])

if __name__ == "__main__":
    p = argparse.ArgumentParser(description="1-2 内置搜索版（mimo/zhipu）")
    p.add_argument("question")
    p.add_argument("--backend", choices=list(BACKENDS), default="zhipu")
    p.add_argument("--timeout", type=float, default=180.0)
    a = p.parse_args()
    run(a.backend, a.question, a.timeout)
```

**运行**（在 `chapter1\web-search-agent\` 下）：
```powershell
python main_builtin_search.py "2024年诺贝尔物理学奖获得者是谁？" --backend zhipu
# python main_builtin_search.py "..." --backend mimo   （可选，需 MIMO_API_KEY 与插件开通）
```
**方案 B · 进阶（可选）**：若想在原 agent.py 里原生支持，改动点位清单——`__init__`（L128-L131 跳过 resolve_llm_backend 直接用传入 key/url）、`_get_tools`（L169 返回工具声明而非 GET Formula）、`_execute_formula`（L232 弃用）、`_chat`（L337 tools 换声明）、`search_and_answer`（L408 tool_calls 分支按所选机制处理）。建议先跑方案 C/A 独立版吃透差异再考虑。

### R5 · 实验 7-2：llm_agent.py 加 provider 分支（⭐⭐）

改动点 1 —— [llm_agent.py#L90-L106](chapter1/learning-from-experience/llm_agent.py#L90-L106)，在 `if requested_provider == "dashscope":` 分支**之后**、`else:` 之前插入：

```python
        elif requested_provider in {"zhipu", "deepseek"}:
            _defaults = {
                "zhipu": ("https://open.bigmodel.cn/api/paas/v4", "ZHIPU_API_KEY", "glm-5.3-flash"),
                "deepseek": ("https://api.deepseek.com", "DEEPSEEK_API_KEY", "deepseek-v4-flash"),
            }[requested_provider]
            resolved_base_url, _key_env, _default_model = _defaults
            self.api_key = api_key or os.getenv(_key_env)
            self.model = model if model != "kimi-k3" else _default_model
            self.using_openrouter = False
            self.provider = requested_provider
```

（`self.client` 的构造在 L111 统一用 `self.api_key + resolved_base_url`，插入的分支只需把这几个变量备好，后面的公共路径自然生效。）

改动点 2 —— [experiment.py#L177-L178](chapter1/learning-from-experience/experiment.py#L177-L178)，把 api_key 的三元选择扩展为按 provider 查表：

```python
        provider = os.getenv("LLM_PROVIDER", "moonshot").lower()
        _KEY_ENV = {"dashscope": "DASHSCOPE_API_KEY", "qwen": "DASHSCOPE_API_KEY",
                    "bailian": "DASHSCOPE_API_KEY", "zhipu": "ZHIPU_API_KEY",
                    "deepseek": "DEEPSEEK_API_KEY"}
        api_key = os.getenv(_KEY_ENV.get(provider, "MOONSHOT_API_KEY"))
```

**注意**：llm_agent.py#L19-L24 的温度函数对非 kimi-k3/gpt-5 模型会用请求值 temperature=0.7；glm-5.3-flash 官方温度范围 (0.0, 1.0]（默认 1），0.7 在界内可直接跑。
**边界**：正式验收 `run_experiment_8_2.py` 拒绝任何替代模型（README 明示），替换只用于 `experiment.py` 探索模式。

---

## 4. 第三步：替换后验证清单（V1–V5）

| # | 命令（先 cd 进对应目录） | 通过标准 |
|---|---|---|
| V1 | `python main.py --provider zhipu --model glm-5.3-flash --mode ablation`（1-1） | 正常产出消融三件套（png/md/json），无 401 / 模型不存在 |
| V2 | `python main.py --route workflow --requirement windowsill-plant`（1-4） | outputs\<run_id>\calls\ 里改写记录的 model=glm-5.3-flash，出图成功 |
| V3 | `python main.py --route native_gptimage --requirement agi-programmer`（1-4） | calls 里 model=agnes-image-2.0-flash，images\ 出图 |
| V4 | `python main_react_search.py "比特币现价" --model zhipu`（1-2 方案 C；快路径 `python main_builtin_search.py "比特币现价" --backend zhipu`） | 方案 C：多轮 🔧/👀 轨迹 + 带来源答案；方案 A：答案含实时价格 + 引用 |
| V5 | `$env:LLM_PROVIDER="zhipu"; python experiment.py --mode llm --llm-episodes 1`（7-2，PowerShell 设置 env） | LLM 决策过程正常打印一局；429/400 按注释处理 |

---

## 5. 边界与警告

1. **1-3 不替换模型（唯一例外，两层论见 §5.1）**：协议层智谱其实有 Responses 端点，但验收绑定 DashScope 托管工具回执；补一个 DASHSCOPE key 跑原模型即可，代码零改动。
2. **正式验收脚本不许换模型**：`run_experiment_8_2.py`、`run_experiment_1_2.py` 等验收 runner 的证据语义绑定原 provider。
3. **密钥安全**：所有 key 只进根 `.env`；测试脚本从 env 读，不硬编码；改动文件与 `model_tests\` 保持本地，不 commit、不 PR 回上游。
4. **以本地为准**：若行号与本指南有 ±几行偏差（仓库可能更新），按函数名/注释定位；冲突时以你本地的实际代码为裁判。

### 5.1 1-3 能不能全走 glm-5.3-flash：代码不锁模型，悬念只在"托管工具层"（T6 定夺）

你的判断"5.3-flash 应该也支持 Responses"**在协议层是对的**（智谱官方有 `https://open.bigmodel.cn/api/v1`，[GLM-5.3 模型页](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.3)）；经逐行复核，**1-3 的代码也确实不锁模型、不锁平台**——我此前"只能百炼"的结论过于绝对，已收回：

- `--backend dashscope` 只是"第二个配置槽"：key / base_url / model 全部来自 env（[config.py#L63-L64](chapter1/search-codegen/config.py#L63-L64)），`--model` 参数可直接覆盖（main.py 的 `Config.resolve(args.backend, args.model)`）
- 验证器的模型身份检查是"请求的 == 返回的"**相对检查**（[run_experiment_1_3.py#L120-L125](chapter1/search-codegen/run_experiment_1_3.py#L120-L125)），**不写死 qwen3.7-plus**；后端白名单 `ACCEPTANCE_BACKENDS = ("openai", "dashscope")`（[#L51](chapter1/search-codegen/run_experiment_1_3.py#L51)）按 flag 名记录——把 dashscope 槽指到智谱，记录名仍是 dashscope，能过这道门
- 指向智谱时（base_url 不含 "dashscope"），agent.py 自动改用 OpenAI 风格托管工具声明 + 非流式请求（[agent.py#L65-L76](chapter1/search-codegen/agent.py#L65-L76)、L99-L109）

**唯一真正的硬门槛**：响应里必须有**平台服务端代跑**产生的 `web_search_call` / `code_interpreter_call` 回执项（[#L137-L140](chapter1/search-codegen/run_experiment_1_3.py#L137-L140)；README#L51-L54、L95-L97）。这是平台能力，不是模型能力——模型换谁都行，平台必须真代跑：

| 层 | 是什么 | 谁有 |
|---|---|---|
| ① 协议层 | `/v1/responses` 请求格式 | OpenAI、智谱（`/api/v1`）、DeepSeek V4-Pro、DashScope |
| ② 托管工具层 | 平台代跑 `web_search` / `code_interpreter` 并产出回执项 | 百炼/OpenAI **已实测**；**智谱 `/v1/responses` 未验证**——T6 十分钟出真相 |

**决策树**：
- **T6 通过**（智谱代跑）→ 1-3 全程 glm-5.3-flash + 智谱，百炼不用注册。`.env` 把 dashscope 槽指向智谱：
  ```env
  DASHSCOPE_API_KEY=<你的ZHIPU_API_KEY>
  DASHSCOPE_BASE_URL=https://open.bigmodel.cn/api/v1
  DASHSCOPE_MODEL=glm-5.3-flash
  ```
  然后：
  ```powershell
  cd chapter1\search-codegen
  python main.py --backend dashscope --mode single --request "东盟 10 国首都之间最近的一对是哪两个？请搜索并用 Python 计算" --output result.json
  ```
  （若在 `store`/`background` 字段上报错，把报错贴我——那是 agent.py 对 custom provider 的请求形状问题，有对应解法）
- **T6 失败**（智谱只推理不代跑 / 报错）→ 完整验收退回百炼兜底（见下）；或接受"无回执学习版"——flash 跑得通流程但不算 repo 验收语义
- **T6 被挡（实测现状 2026-09-13）**：两发探针均 429 `permission_denied`（"GLM Coding Plan 套餐已到期"）——`/api/v1` 是 Coding Plan 专属端点，现有 key 已过期。**三选一（本人操作）**：① 续订 GLM Coding Plan 后重跑 T6；② 在 bigmodel.cn 开放平台注册**普通按量 API key**（flash 0.8/2.8 元每 M）加入 `.env` 后用它重测 T6；③ 不折腾智谱 → 百炼兜底或无回执学习版。paas/v4 不受影响（T2 均通、1-1 已跑通）。

**百炼兜底（T6 失败时才需要）**——按 [search-codegen/env.example#L15-L21](chapter1/search-codegen/env.example#L15-L21)，`.env` 加：

```env
DASHSCOPE_API_KEY=<百炼/DashScope key>
# 国内注册的 key 用：
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_MODEL=qwen3.7-plus
```

然后（[README#L128-L132](chapter1/search-codegen/README.md#L128-L132)）：

```powershell
cd chapter1\search-codegen
python main.py --backend dashscope --mode single --request "东盟 10 国首都之间最近的一对是哪两个？请搜索并用 Python 计算" --output result.json
```

无 key 时先看结构：`python main.py --backend openai --dry-run --request "东盟 10 国首都之间最近的一对？"`（不发请求）。验收基准：东盟答案 **316.35 km**（[#L77-L80](chapter1/search-codegen/README.md#L77-L80)）。
